env_config:
  extend:
    maze_store: ${extend:${include:configs_mujoco/mazes/dr/MAZE_STORE.yaml}}

  maze_selection_criteria:
    mode: CYCLE
  maze_configs:
    # Only statics for training
    - Y_MAZE_STATIC