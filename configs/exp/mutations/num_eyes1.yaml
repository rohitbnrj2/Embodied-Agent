# @package _global_

hydra:
  sweeper:
    params:
      env.agents.agent.num_eyes_to_generate.1:
        init: ${env.agents.agent.num_eyes_to_generate.1}
        lower: 1
        upper: 50
        integer: true
