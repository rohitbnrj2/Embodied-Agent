# @package _global_

hydra:
  sweeper:
    params:
      env.agents.agent.eyes.eye.resolution:
        init: ${env.agents.agent.eyes.eye.resolution}
        lower: 1
        upper: 100
        integer: true
