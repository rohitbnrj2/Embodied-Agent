# @package _global_

hydra:
  sweeper:
    params:
      env.animals.animal.eyes.eye.resolution:
        init: ${env.animals.animal.eyes.eye.resolution}
        lower: 1
        upper: 100
        integer: true
