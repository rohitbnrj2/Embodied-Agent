# @package _global_

hydra:
  sweeper:
    params:
      env.animals.animal.num_eyes_to_generate.1:
        init: ${env.animals.animal.num_eyes_to_generate.1}
        lower: 1
        upper: 100
        integer: true
