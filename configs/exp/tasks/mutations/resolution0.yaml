# @package _global_

hydra:
  sweeper:
    params:
      env.animals.animal.eyes.eye.resolution.0:
        init: ${env.animals.animal.eyes.eye.resolution.0}
        lower: 1
        upper: 100
        integer: true
