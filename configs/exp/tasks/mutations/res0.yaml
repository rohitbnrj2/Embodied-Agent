# @package _global_

hydra:
  sweeper:
    params:
      env.agents.agent.eyes.eye.resolution.0:
        init: ${env.agents.agent.eyes.eye.resolution.0}
        lower: 1
        upper: 50
        integer: true
