# @package _global_

# NOTE: Remember to set eyes_lon_range.0 equal to eyes_lon_range.1 in the config

hydra:
  sweeper:
    params:
      env.agents.agent.eyes_lon_range.1:
        init: ${env.agents.agent.eyes_lon_range.1}
        lower: 0
        upper: 180
