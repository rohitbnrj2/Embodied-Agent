# @package _global_

hydra:
  sweeper:
    params:
      env.animals.animal.num_eyes_to_generate:
        init: ${env.animals.animal.num_eyes_to_generate}
        lower: 1
        upper: 100
        integer: true
