# @package _global_

defaults:
  - exp

  - /exp/mutations:
      # - weights
      - resolution
      - num_eyes
      - lon_range
      - lat_range
      - stack_size
      - fov

  - override /task: detection
