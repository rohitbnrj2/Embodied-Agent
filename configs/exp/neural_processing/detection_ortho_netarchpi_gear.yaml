# @package _global_

defaults:
  - /exp/tasks/mutations/netarchpi

  - /exp/neural_processing/detection_ortho
  - override /exp/tasks/initial_conditions: simple_flatland

hydra:
  sweeper:
    params:
      custom.gear:
        init: 0.01
        lower: 0.01
        upper: 2.0
