# @package _global_

defaults:
  - /exp/neural_processing/detection_stacksize

env:
  agents:
    agent:
      xml:
        overrides:
          - mujoco:
              - actuator:
                  - position:
                      - name: "${parent:xml}_act_yaw"
                      - gear: "${custom.gear}"

custom:
  gear: "???"
