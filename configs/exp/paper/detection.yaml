# @package _global_

defaults:
  - /exp/environmental_effects/detection_num_eyes_and_resolution@_here_
  - /exp/paper/mazes/FIXED_OPEN@env.mazes.OPEN

custom:
  frequency: 8

env:
  termination_fn:
    terminate_if_close_to_goal:
      distance_threshold: 1.0
