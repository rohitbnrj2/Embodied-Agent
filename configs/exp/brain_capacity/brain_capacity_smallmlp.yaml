# @package _global_

defaults:
  - /exp/tasks/initial_conditions/simple_flatland

trainer:
  model:
    policy_kwargs:
      features_extractor_kwargs:
        output_dim: 1
        image_extractor:
          architecture: [1]
