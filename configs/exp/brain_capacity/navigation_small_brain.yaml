# @package _global_

defaults:
  - /exp/brain_capacity/navigation_brain_capacity_mlp@_here_

trainer:
  # Each eye will have exactly 1 neuron and 1 output. This is the minimum allowed for
  # our feature extractor.
  model:
    policy_kwargs:
      features_extractor_kwargs:
        output_dim: 1
        image_extractor:
          architecture: [1]