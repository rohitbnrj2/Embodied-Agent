version: 1
disable_existing_loggers: False
formatters:
  simple:
    format: '%(message)s'
handlers:
  stdout:
    class: logging.StreamHandler
    formatter: simple
    stream: ext://sys.stdout
    level: DEBUG 
loggers:
  cambrian:
    level: DEBUG
    handlers: [stdout]
