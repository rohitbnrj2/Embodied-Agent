maze_configs_store:
  BASE:
    extend: 
      base: ${extend:${include:configs/mazes/maze.yaml}}
    wall_texture_map:
      default: [vertical_10]
      L: [vertical_10] # left
      R: [vertical_10] # right
      E: [vertical_10] # end
  I_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/I_MAZE_STATIC.yaml}}
  I_MAZE_LARGE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/I_MAZE_LARGE_STATIC.yaml}}
    wall_texture_map:
      L: [vertical_20] # left
      R: [vertical_10] # right
    size_scaling: 10.0
  I_MAZE_LARGE_STATIC_FLIP:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/I_MAZE_LARGE_STATIC.yaml}}
    wall_texture_map:
      L: [vertical_10] # left
      R: [vertical_20] # right
    size_scaling: 10.0
  I_MAZE_INVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/I_MAZE_INVERSE.yaml}}
    ref: I_MAZE_STATIC
  I_MAZE_STATIC_SWITCH:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/I_MAZE_STATIC_SWITCH.yaml}}
    wall_texture_map:
      L: [vertical_10] # left
      R: [vertical_20] # right
    size_scaling: 10.0
  I_MAZE_DIVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/I_MAZE_DIVERSE.yaml}}
    ref: I_MAZE_STATIC
  L_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/L_MAZE_STATIC.yaml}}
  L_MAZE_CHOICE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/L_MAZE_CHOICE.yaml}}
    ref: L_MAZE_STATIC
  L_MAZE_INVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/L_MAZE_INVERSE.yaml}}
    ref: L_MAZE_STATIC
  L_MAZE_DIVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/L_MAZE_DIVERSE.yaml}}
    ref: L_MAZE_STATIC
  U_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/U_MAZE_STATIC.yaml}}
  U_MAZE_INVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/U_MAZE_INVERSE.yaml}}
    ref: U_MAZE_STATIC
  U_MAZE_DIVERSE:
    extend:
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/U_MAZE_DIVERSE.yaml}}
    ref: U_MAZE_STATIC
  S_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/S_MAZE_STATIC.yaml}}
  S_MAZE_VARIABLE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/S_MAZE_VARIABLE.yaml}}
    ref: S_MAZE_STATIC
  S_MAZE_INVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/S_MAZE_INVERSE.yaml}}
    ref: S_MAZE_STATIC
  S_MAZE_DIVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/S_MAZE_DIVERSE.yaml}}
    ref: S_MAZE_STATIC
  X_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/X_MAZE_STATIC.yaml}}
  X_MAZE_CHOICE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/X_MAZE_CHOICE.yaml}}
    ref: X_MAZE_STATIC
  Y_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_MAZE_STATIC.yaml}}
  Y_MAZE_CHOICE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_MAZE_CHOICE.yaml}}
    ref: Y_MAZE_STATIC
  Y_MAZE_CHOICE_FLIPPED:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_MAZE_CHOICE_FLIPPED.yaml}}
  Y_MAZE_OPTION:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_MAZE_OPTION.yaml}}
    ref: Y_MAZE_STATIC
  Y_MAZE_DIVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_MAZE_DIVERSE.yaml}}
    ref: Y_MAZE_STATIC
  Y_COMPLEX_MAZE_OPTION:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_COMPLEX_MAZE_OPTION.yaml}}
  Y_COMPLEX_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/dr/Y_COMPLEX_MAZE_STATIC.yaml}}
    ref: Y_COMPLEX_MAZE_OPTION
  CAMBRIAN_MAZE:
    extend: 
      base: ${extend:${include:configs/mazes/dr/CAMBRIAN_MAZE.yaml}}
  OBS_AVOID_STRAIGHT_1:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/obstacle_avoidance/STRAIGHT_1.yaml}}
  OBS_AVOID_STRAIGHT_2:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/obstacle_avoidance/STRAIGHT_2.yaml}}
  OBS_AVOID_STRAIGHT_3:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/obstacle_avoidance/STRAIGHT_3.yaml}}
  OBS_AVOID_STRAIGHT_4:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/obstacle_avoidance/STRAIGHT_4.yaml}}
  OBS_AVOID_STRAIGHT_5:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/obstacle_avoidance/STRAIGHT_5.yaml}}
  OBS_AVOID_STRAIGHT_6:
    extend: 
      base: ${extend:${include:configs/mazes/obstacle_avoidance/STRAIGHT_6.yaml}}
  OBS_AVOID_SHORT_1:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/obstacle_avoidance/SHORT_1.yaml}}
  LARGE_MAZE_STATIC:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/train/LARGE_MAZE_STATIC.yaml}}
  LARGE_MAZE_DIVERSE:
    extend: 
      ref: ${extend:${...BASE}}
      base: ${extend:${include:configs/mazes/train/LARGE_MAZE_DIVERSE.yaml}}
    ref: LARGE_MAZE_STATIC
  OPTIC_FLOW_STRAIGHT:
    extend: 
      base: ${extend:${include:configs/mazes/eval/OPTIC_FLOW_STRAIGHT.yaml}}
      ref: ${extend:${...BASE}}
  OPTIC_FLOW_TAPER:
    extend: 
      base: ${extend:${include:configs/mazes/eval/OPTIC_FLOW_TAPER.yaml}}
      ref: ${extend:${...BASE}}
  OPTIC_FLOW_INVERSE_TAPER:
    extend: 
      base: ${extend:${include:configs/mazes/eval/OPTIC_FLOW_STRAIGHT_TEXTURE.yaml}}
      ref: ${extend:${...BASE}}
  OPTIC_FLOW_TAPER_AND_INVERSE_TAPER:
    extend: 
      base: ${extend:${include:configs/mazes/eval/OPTIC_FLOW_TAPER_AND_INVERSE_TAPER.yaml}}
      ref: ${extend:${...BASE}}
  
