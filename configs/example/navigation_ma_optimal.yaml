# @package _global_

defaults:
  - navigation_ma

  # Override the agents to use an "optimal" policy
  - override /env/agents@env.agents.agent: point_seeker_maze
  - override /env/agents@env.agents.agent1: point_seeker_maze

env:
  agents:
    agent:
      instance:
        target: goal
    agent1:
      instance:
        target: goal
