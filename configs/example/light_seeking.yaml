# @package _global_

defaults:
  - example
  - override /task: light_seeking

env:
  agents:
    agent:
      eyes:
        eye:
          resolution: [1, 1]
          num_eyes: [1, 1]
          fov: [30, 150]
