# @package _global_

defaults:
  - example
  - override /task: detection

env:
  agents:
    agent:
      eyes:
        eye:
          resolution: [10, 10]
          # num_eyes: [1, 3]
          # lon_range: [-30, 30]
