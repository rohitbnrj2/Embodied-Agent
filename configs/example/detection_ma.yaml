# @package _global_

defaults:
  - example
  - override /task:
      - detection
      - multi_agent

env:
  agents:
    agent:
      instance:
        target: goal0

      eyes:
        eye:
          resolution: [20, 20]
          num_eyes: [1, 3]
          lon_range: [-30, 30]

    agent1:
      instance:
        target: goal0

      eyes:
        eye:
          resolution: [20, 20]
          num_eyes: [1, 3]
          lon_range: [-30, 30]
