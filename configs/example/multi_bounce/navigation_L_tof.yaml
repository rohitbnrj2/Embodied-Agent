# @package _global_

defaults:
  - /example/multi_bounce/navigation_L

  - override /env/agents/eyes@env.agents.agent.eyes.eye: tof_eye_3b

env:
  agents:
    agent:
      eyes:
        eye:
          coord: [-5, 0]
          resolution: [2, 2]
