# @package _global_

# Similar to tracking problem, but the adversaries have a fixed policy which tries to
# catch the agent.

defaults:
  # Inherit the config from tracking
  - tracking

  # Define an adversarial predator which tries to catch the agent
  - override /env/agents@env.agents.adversary0: point_predator_textured
