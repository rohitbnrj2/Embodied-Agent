defaults:
  - /default/${env/animals/default}@_here_

instance:
  _target_: cambrian.animals.point.MjCambrianPointAnimal
  _partial_: true

xml:
  _target_: cambrian.utils.cambrian_xml.convert_xml_to_yaml
  _convert_: all
  base_xml_path: models/animals/point.xml

body_name: "particle_${parent:}"
joint_name: "ball_x_${parent:}"
geom_name: "particle_geom_${parent:}"

eyes_lat_range: [-5, 25]
eyes_lon_range: [-60, 60]

mutations_from_parent: []
