# @package custom.default.animal
# We aren't actually going to use this animal, so store it in the special attr `custom` which isn't read

defaults:
  - point@_here_

use_privileged_action: true

instance:
  _target_: cambrian.animals.MjCambrianPointAnimalMazeOptimal
  _partial_: true
  target_object: goal # assumes goal, can change via overrides