# @package custom.default.object
# We aren't actually going to use this object, so store it in the special attr `custom` which isn't read

terminate_if_close: false
truncate_if_close: false
reward_if_close: 0.0
distance_to_target_threshold: 2.0

use_as_obs: false

pos:
  _target_: numpy.array
  object: [0, 0, 0] # placeholder; should be updated by the environment
