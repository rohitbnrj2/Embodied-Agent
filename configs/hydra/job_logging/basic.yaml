defaults:
  - logging
  - stderr
  - custom_stdout
  - file

loggers:
  cambrian:
    handlers: [file_out, file_err, stdout_debug, stderr]
root:
  LEVEL: INFO
  handlers: [file_out, file_err, stdout_info, stderr]
