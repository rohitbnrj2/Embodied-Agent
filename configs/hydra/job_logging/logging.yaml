version: 1
formatters:
  simple:
    format: '%(levelname)-8s | %(module)s.%(funcName)s :: %(message)s'
root:
  level: DEBUG

# NOTE: unclear what the consequences of this are
disable_existing_loggers: true
