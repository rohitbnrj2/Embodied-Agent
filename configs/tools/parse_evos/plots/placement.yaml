lon_diff_vs_generation:
  xdata:
    type: GENERATION
  ydata:
    type: CUSTOM
    label: Longitudinal Difference
    custom_fn: 
      _target_: custom_axis_fns.eye_range_diff
      _partial_: True
      pattern: env.animals.*.eyes_lon_range
lon_mean_vs_generation:
  xdata:
    type: GENERATION
  ydata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range
    label: Longitudinal Mean
lon0_vs_generation:
  xdata:
    type: GENERATION
  ydata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[0]
    label: Longitudinal Min Range
training_reward_vs_lon0:
  xdata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[0]
    label: Longitudinal Min Range
  ydata:
    type: MONITOR
fitness_vs_lon0:
  xdata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[0]
    label: Longitudinal Min Range
  ydata:
    type: EVALUATION
fitness_vs_lon_diff:
  xdata:
    type: CUSTOM
    label: Longitudinal Range
    custom_fn: 
      _target_: custom_axis_fns.eye_range_diff
      _partial_: True
      pattern: env.animals.*.eyes_lon_range
  ydata:
    type: EVALUATION
training_reward_vs_lon_diff:
  xdata:
    type: CUSTOM
    label: Longitudinal Range
    custom_fn: 
      _target_: custom_axis_fns.eye_range_diff
      _partial_: True
      pattern: env.animals.*.eyes_lon_range
  ydata:
    type: MONITOR
fitness_vs_lon_mean:
  xdata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range
    label: Longitudinal Mean
  ydata:
    type: EVALUATION
training_reward_vs_lon_mean:
  xdata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range
    label: Longitudinal Mean
  ydata:
    type: MONITOR
lon0_vs_lon1:
  xdata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[0]
    label: Longitudinal Min Range
  ydata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[1]
    label: Longitudinal Max Range
lon0_vs_lon1_vs_training_reward_color:
  xdata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[0]
    label: Longitudinal Min Range
  ydata:
    type: CONFIG
    pattern: env.animals.*.eyes_lon_range[1]
    label: Longitudinal Max Range
  color_data:
    type: MONITOR