number_of_eyes_vs_generation:
  x_data:
    type: GENERATION
  y_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
number_of_eyes_vs_generation_vs_training_reward_color:
  x_data:
    type: GENERATION
  y_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  use_average_line: True
  color_data:
    type: MONITOR
number_of_eyes_vs_geneeration_vs_training_reward_size_vs_generation_color:
  x_data:
    type: GENERATION
  y_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  size_data:
    type: MONITOR
  color_data:
    type: GENERATION
    cmap: magma
width_vs_number_of_eyes:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: CONFIG
    pattern: env.animals.*.eyes.*.resolution[0]
    label: Width
width_vs_number_of_eyes_vs_generation_color:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: CONFIG
    pattern: env.animals.*.eyes.*.resolution[0]
    label: Width
  color_data:
    type: GENERATION
    cmap: viridis
width_vs_number_of_eyes_vs_training_reward_color_vs_generation_size:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: CONFIG
    pattern: env.animals.*.eyes.*.resolution[0]
    label: Width
  color_data:
    type: MONITOR
    cmap: magma
  size_data:
    type: GENERATION
width_vs_number_of_eyes_vs_training_reward_color:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: CONFIG
    pattern: env.animals.*.eyes.*.resolution[0]
    label: Width
  color_data:
    type: MONITOR
training_reward_vs_number_of_eyes:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: MONITOR
training_reward_vs_number_of_eyes_vs_generation_color:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: MONITOR
  color_data:
    type: GENERATION
    cmap: viridis
fitness_vs_number_of_eyes:
  x_data:
    type: CUSTOM
    label: Number of Eyes
    custom_fn:
      _target_: custom_axis_fns.eval_safe
      _partial_: True
      src: math.prod(num_eyes_to_generate[0])
      patterns:
        num_eyes_to_generate: env.animals.*.num_eyes_to_generate
  y_data:
    type: EVALUATION